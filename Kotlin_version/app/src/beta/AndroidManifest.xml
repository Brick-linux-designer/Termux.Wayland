<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TermuxWayland">

        <activity
            android:name=".WaylandSplashScreen"
            android:exported="true"
            android:theme="@style/Theme.TermuxWayland">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.TermuxWayland" />

        <receiver 
            android:name=".widgets.DockWidget" 
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dock_widget_info" />
        </receiver>

        <receiver 
            android:name=".widgets.WidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.termux.wayland.kotlin.beta.ACTION_DOCK_CLICK" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
